{
  "rules": {
    ".write": false,
    ".read": false,
    "games": {
      ".write": "newData.exists() && !data.exists()",
      "$game_id": {
        ".read": true,
        ".write": true
      }
    }
  }
}
